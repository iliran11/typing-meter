//https://stackoverflow.com/questions/46954647/react-redux-redirect-to-other-page-after-login

import createHistory from 'history/createBrowserHistory'

const history = createHistory()

export default history
